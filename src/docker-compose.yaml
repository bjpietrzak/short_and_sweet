version: '0'
services:
  backend:
    build: backend/
    image: backend
    ports:
      - "7001:7001"
    volumes:
      - ./backend/app:/app
    restart: always
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "7001"]
