version: '0'
services:
  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    image: backend
    ports:
      - "7001:7001"
    restart: always
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "7001"]
